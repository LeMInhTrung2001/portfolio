import { Html } from "@react-three/drei"


export const Loader = () => {
  // const {progress} = useProgress();
  return (
    <Html center className="font-blade text-xl text-center">
      {/* {progress}% Loader */}
      Loading...
    </Html>
  )
}
